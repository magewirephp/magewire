<script>
    document.addEventListener('livewire:init', () => {
        Livewire.hook('morph', ({ el, toEl }) => {
            if (el.classList.contains('scroll-reveal')) {
                toEl.classList.add(...[...el.classList]);
            }
        });
    });
</script>
