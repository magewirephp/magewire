<?php

declare(strict_types=1);

use Magento\Framework\View\Element\Template;

/** @var Template $block */

$variants = [
    'first' => ['label' => 'First compiled item', 'enabled' => true],
    'hidden' => ['label' => 'Hidden compiled item', 'enabled' => false],
    'last' => ['label' => 'Last compiled item', 'enabled' => true]
];
?>
<section
    data-testid="compiler-default"
    data-compiler="<?= $escaper->escapeHtmlAttr($block->getData('magewire:compiler')::class) ?>"
    class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"
>
    <h2 class="mb-4 text-xl font-bold">Default compiler</h2>
    <ul class="space-y-2">
        @foreach($variants as $key => $variant)
            @if($variant['enabled'])
                <li data-variant-key="{{ attr($key) }}">{{ $variant['label'] }}</li>
            @endif
        @endforeach
    </ul>
</section>
